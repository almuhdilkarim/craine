
function raptor_kerns_prep_install() {
    arch-chroot /mnt "curl -s 'https://liquorix.net/install-liquorix.sh' | sudo bash"
}

function raptor_kerns_prep() {
    
    sleep 2 && clear
    echo "Kernel module installation"

    # raptor_kerns_prep_install
}
